import { AnimateCountDirective } from './animate-count.directive';

describe('AnimateCountDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateCountDirective();
    expect(directive).toBeTruthy();
  });
});
